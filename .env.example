# Triage Playbook environment config
# Copy to .env (never commit)

# Supabase (public-safe, used in frontend)
SUPABASE_URL=https://tfizrgevpajadduuzhdl.supabase.co
SUPABASE_ANON_KEY=replace_with_anon_key

# Netlify Functions (server-only, never expose)
SUPABASE_SERVICE_ROLE_KEY=replace_with_service_role_key
ANTHROPIC_API_KEY=replace_with_anthropic_key
RESEND_API_KEY=replace_with_resend_key
EMAIL_FROM=noreply@ikigaios.com

# Optional
SLACK_WEBHOOK_URL=
